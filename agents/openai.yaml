interface:
  display_name: "Repo Skill Generator"
  short_description: "Create source-invisible capability skills from projects."
  default_prompt: "Use $repo-skill-generator to fully scan this project, propose source-neutral capability modules and functions for my approval, then create a Codex, Claude, or OpenCode skill whose abilities do not expose the source project."
